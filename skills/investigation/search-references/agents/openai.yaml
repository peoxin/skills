interface:
  display_name: "search-references"
  short_description: "Find research references for selection"
  default_prompt: "Use the `search-references` skill to find traceable sources, present a shortlist, and hand selected inputs to the `add-references` skill."
