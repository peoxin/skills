interface:
  display_name: "define-experiment"
  short_description: "Define one concrete PyTorch Experiment"
  default_prompt: "Use $define-experiment to define an Experiment with selected component paths, phases, project-native configuration or custom phase code, and outputs."
