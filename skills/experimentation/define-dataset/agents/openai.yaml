interface:
  display_name: "define-dataset"
  short_description: "Define a self-contained, reproducible Dataset component"
  default_prompt: "Use $define-dataset to align a Dataset revision and derivation, write the component contracts, and implement the required processing."
